{% comment %}
  Home Endorsement Section - 网红推荐区域
  使用 blocks 实现，避免 schema 报错，兼容所有 Shopify 主题
{% endcomment %}

<style>
  .endorsement-section {
    padding: 4rem 0;
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  }

  .endorsement-container {
    max-width: 1280px;
    width: 90%;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .endorsement-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .endorsement-title {
    font-size: 2.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
    color: #1f2937;
  }

  .endorsement-subtitle {
    font-size: 1.25rem;
    color: #6b7280;
    max-width: 48rem;
    margin: 0 auto;
    line-height: 1.6;
  }

  .endorsement-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    margin-top: 2rem;
  }

  .endorsement-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  .endorsement-tab {
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-weight: 500;
    border: none;
    cursor: pointer;
    background: #e2e8f0;
    color: #475569;
    transition: all 0.3s ease;
    font-size: 0.875rem;
  }

  .endorsement-tab.active {
    background: #9abd48;
    color: white;
    box-shadow: 0 4px 6px rgba(154, 189, 72, 0.2);
  }

  .endorsement-card {
    background: white;
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .endorser-profile {
    display: flex;
    align-items: center;
    margin-bottom: 1.5rem;
  }

  .endorser-avatar {
    width: 4rem;
    height: 4rem;
    border-radius: 50%;
    object-fit: cover;
    margin-right: 1rem;
  }

  .endorser-info h3 {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 0.25rem;
    color: #1f2937;
  }

  .endorser-title {
    color: #6b7280;
    margin-bottom: 0.25rem;
  }

  .endorser-social {
    display: flex;
    align-items: center;
    font-size: 0.875rem;
    color: #6b7280;
  }

  .endorser-social i {
    margin-right: 0.25rem;
  }

  .endorsement-quote {
    font-style: italic;
    color: #4b5563;
    line-height: 1.7;
    font-size: 1.125rem;
  }

  .endorsement-image {
    width: 100%;
    height: 24rem;
    object-fit: cover;
    border-radius: 1rem;
  }

  @media (max-width: 768px) {
    .endorsement-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .endorsement-title {
      font-size: 2rem;
    }

    .endorsement-subtitle {
      font-size: 1.125rem;
    }

    .endorsement-tabs {
      justify-content: center;
    }
  }
</style>

<section class="endorsement-section">
  <div class="endorsement-container">
    <div class="endorsement-header">
      <h2 class="endorsement-title">{{ section.settings.title }}</h2>
      <p class="endorsement-subtitle">{{ section.settings.subtitle }}</p>
    </div>

    <div class="endorsement-content">
      <div>
        <!-- 选项卡 -->
        <div class="endorsement-tabs">
          {% for block in section.blocks %}
            {% if block.type == 'endorser' %}
              <button
                class="endorsement-tab {% if forloop.index0 == 0 %}active{% endif %}"
                data-index="{{ forloop.index0 }}"
              >
                {{ block.settings.name }}
              </button>
            {% endif %}
          {% endfor %}
        </div>

        <!-- 推荐卡片 -->
        <div class="endorsement-card" id="endorsementCard">
          {% assign first_block = section.blocks | where: 'type', 'endorser' | first %}
          {% if first_block %}
            <div class="endorser-profile">
              <img
                src="{{ first_block.settings.image | image_url: width: 200 }}"
                alt="{{ first_block.settings.name }}"
                class="endorser-avatar"
              >
              <div class="endorser-info">
                <h3>{{ first_block.settings.name }}</h3>
                <p class="endorser-title">{{ first_block.settings.title }}</p>
                <div class="endorser-social">
                  <i class="fab fa-instagram"></i>
                  <span>{{ first_block.settings.social_handle }} • {{ first_block.settings.followers }} followers</span>
                </div>
              </div>
            </div>
            <p class="endorsement-quote">"{{ first_block.settings.quote }}"</p>
          {% endif %}
        </div>
      </div>

      <!-- 右侧大图 -->
      <div>
        <img
          src="{% if first_block %}{{ first_block.settings.image | image_url: width: 600 }}{% else %}//via.placeholder.com/600x400{% endif %}"
          alt="Endorsement"
          class="endorsement-image"
          id="endorsementImage"
        >
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const tabs = document.querySelectorAll('.endorsement-tab');
    const card = document.getElementById('endorsementCard');
    const image = document.getElementById('endorsementImage');

    // 将每个 block 转为 JS 对象
    const endorsements = [
      {% for block in section.blocks %}
        {% if block.type == 'endorser' %}
          {
            image: "{{ block.settings.image | img_url: '600x' }}",
            name: {{ block.settings.name | json }},
            title: {{ block.settings.title | json }},
            social_handle: {{ block.settings.social_handle | json }},
            followers: {{ block.settings.followers | json }},
            quote: {{ block.settings.quote | json }}
          }{% unless forloop.last %},{% endunless %}
        {% endif %}
      {% endfor %}
    ];

    function updateEndorsement(index) {
      const e = endorsements[index];

      // 更新选项卡
      tabs.forEach(tab => tab.classList.remove('active'));
      tabs[index].classList.add('active');

      // 更新内容
      card.innerHTML = `
        <div class="endorser-profile">
          <img src="${e.image}" alt="${e.name}" class="endorser-avatar">
          <div class="endorser-info">
            <h3>${e.name}</h3>
            <p class="endorser-title">${e.title}</p>
            <div class="endorser-social">
              <i class="fab fa-instagram"></i>
              <span>${e.social_handle} • ${e.followers} followers</span>
            </div>
          </div>
        </div>
        <p class="endorsement-quote">"${e.quote}"</p>
      `;

      // 更新图片
      image.src = e.image;
    }

    // 绑定点击事件
    tabs.forEach((tab, index) => {
      tab.addEventListener('click', () => {
        if (index < endorsements.length) {
          updateEndorsement(index);
        }
      });
    });
  });
</script>

{% schema %}
{
  "name": "Home Endorsement Section",
  "settings": [
    {
      "type": "header",
      "content": "Section Title"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Influencer Reviews"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Section Subtitle",
      "default": "Hear what top influencers and lifestyle creators say about our products"
    }
  ],
  "blocks": [
    {
      "type": "endorser",
      "name": "网红推荐人",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "头像图片",
          "info": "建议尺寸：400x400px"
        },
        {
          "type": "text",
          "id": "name",
          "label": "姓名",
          "default": "Sarah Johnson"
        },
        {
          "type": "text",
          "id": "title",
          "label": "职业",
          "default": "Fashion Influencer"
        },
        {
          "type": "text",
          "id": "social_handle",
          "label": "社交媒体账号",
          "default": "@sarahstyles"
        },
        {
          "type": "text",
          "id": "followers",
          "label": "粉丝数",
          "default": "250K"
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "推荐语",
          "default": "Absolutely love the quality and design of these products!"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Home Endorsements",
      "category": "Home Page",
      "settings": {
        "title": "Influencer Reviews",
        "subtitle": "Hear what top influencers and lifestyle creators say about our products"
      },
      "blocks": [
        {
          "type": "endorser",
          "settings": {
            "image": "",
            "name": "Sarah Johnson",
            "title": "Fashion Influencer",
            "social_handle": "@sarahstyles",
            "followers": "250K",
            "quote": "Absolutely love the quality and design of these products! They have become my go-to accessories."
          }
        },
        {
          "type": "endorser",
          "settings": {
            "image": "",
            "name": "Mike Chen",
            "title": "Lifestyle Creator",
            "social_handle": "@mikelifestyle",
            "followers": "180K",
            "quote": "The craftsmanship is exceptional. These pieces truly stand out in my collection."
          }
        }
      ]
    }
  ]
}
{% endschema %}
